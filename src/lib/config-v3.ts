/**
 * Marché de Mo' V3 Storefront Configuration
 * Centralised layout and design toggle.
 * 
 * Set `isV3Active` to true to enable the E.Leclerc-like storefront layout (V3).
 * Set `isV3Active` to false to revert to the original V2 storefront layout.
 */
export const configV3 = {
  isV3Active: true,
};
