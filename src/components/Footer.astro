---
import { resolveV3 } from "@/lib/config-v3";
import FooterV2 from "./FooterV2.astro";
import FooterV3 from "./FooterV3.astro";

const props = Astro.props;
const isV3 = resolveV3(Astro.cookies);
---

{isV3 ? <FooterV3 {...props} /> : <FooterV2 {...props} />}
